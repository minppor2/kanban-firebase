# 수업용 칸반보드 (Firebase 버전) — 기획서

**버전** 1.0
**작성 목적** 프로덕트 방향 정렬 (바이브 코딩 실습용)
**상세 구현 스펙** 이 문서는 PM 수준 기획서입니다. 데이터 모델·보안 규칙 등 구현 상세는 이 문서를 참고해 구현하는 사람(AI)이 Firebase 관례에 맞춰 설계합니다.

교사가 학급별 공지 보드를 운영하고, 학생은 각자의 활동을 기록하는 칸반보드 웹 서비스.

---

## 01. 배경

교사가 학생 개개인의 활동(과제 진행, 독서 기록 등)을 관리할 도구가 마땅치 않습니다. 단체 채팅방이나 공책은 검색이 안 되고, 공개 범위를 조절할 수 없어 학생 입장에서 부담스럽습니다. 트렐로 같은 칸반보드 UX를 빌리되, **학생 개인 기록은 절대 다른 학생에게 노출되지 않는** 교실 전용 구조가 필요합니다.

> **기술 방향**
> React + Vite 프론트엔드에 Firebase(Firestore, Authentication)를 BaaS로 사용합니다. 자체 서버 코드를 따로 두지 않고, 인증·데이터베이스·접근 제어를 Firebase가 관리형 서비스로 대신 맡습니다 — 대신 Firestore 보안 규칙이 실질적인 접근 제어 로직을 담당합니다. 상세 데이터 모델과 규칙 설계는 기술 설계 문서에서 별도로 다룹니다.

## 02. 목표

- 교사가 별도 기술 지식 없이 학급을 만들고 학생을 받을 수 있어야 한다.
- 학생은 코드 입력만으로 즉시 참여하고, 자기 기록 공간을 자동으로 갖는다.
- 학생 간 상호 열람이 원천적으로 불가능해야 한다 (심리적 안전).
- 교사는 공지를 초안 상태로 준비했다가 원하는 시점에 공개할 수 있다.
- 서버 운영비 없이 무료 티어로 지속 가능해야 한다.

### 성공 지표

| 지표 | 목표 |
|---|---|
| 온보딩 | 교사 1인이 안내서 없이 5분 내 학급 개설 |
| 참여율 | 가입 코드 입력 → 참여 성공률 |
| 정보 유출 | 학생 간 열람 사고 0건 |
| 운영비 | 월 $0 (무료 티어 내 운영) |

## 03. 대상 사용자

**교사** — 학급 운영자 1인. 학급 생성, 공지 관리, 전체 학생 보드 열람 권한을 가진다.

**학생** — 가입 코드로 참여. 본인 기록과 교사의 공개 공지만 볼 수 있다.

> **역할 결정 방식**
> 회원가입 시 모든 사용자는 기본적으로 **student** role을 부여받는다. **teacher** 권한은 사용자가 스스로 선택하거나 신청할 수 없으며, Firebase 프로젝트 관리자가 Firestore 콘솔에서 해당 사용자의 `role` 필드를 직접 `teacher`로 변경해야만 부여된다.

## 04. 사용자 스토리

| 우선순위 | 스토리 |
|---|---|
| P0 | 교사로서, 학급을 만들고 가입 코드를 학생에게 공유하고 싶다. |
| P0 | 학생으로서, 가입 코드만으로 학급에 참여하고 내 기록 공간을 바로 갖고 싶다. |
| P0 | 학생으로서, 다른 학생이 내 기록을 볼 수 없어야 눈치 안 보고 기록할 수 있다. |
| P0 | 교사로서, 공지를 비공개로 준비했다가 원할 때 공개로 전환하고 싶다. |
| P1 | 교사로서, 학급의 요일·교시 일정을 등록해 두고 싶다. |
| P1 | 학생으로서, 기록 카드의 순서를 자유롭게 정리하고 싶다. |
| P2 | 사용자로서, Google 계정으로 간편하게 로그인하고 싶다. |
| P2 | 운영자로서, 가입 시 이메일 인증을 거치게 해 계정 도용을 방지하고 싶다. |

## 05. 범위

**포함**
- 이메일/Google 인증
- 학급 생성·참여·삭제
- 교사 공지 보드 + 학생 개인 보드
- 카드 공개/비공개 전환
- 실시간 동기화

**제외**
- 파일/이미지 첨부
- 댓글·채팅·좋아요
- 학급당 복수 교사
- 모바일 네이티브 앱
- 성적·출결 시스템 연동

## 06. 구현 단계 지침

실습(짧은 시간 내 여러 차례 화면 확인) 목적을 고려해, 이번 단계의 구현 범위를 다음과 같이 못박는다.

- **데이터 구조는 Firebase Authentication + Firestore 연동을 전제로 설계한다** — 화면을 만들기 편한 대로 데이터를 깊게 중첩하지 말고, Firestore 컬렉션처럼 프로필·학급·리스트·카드가 각각 독립된 단위로 분리된 구조를 사용한다.
- **단, 로그인 화면과 실제 Firebase Auth 연동은 이번 단계에서 구현하지 않는다.** 대신 고정된 teacher/student 프로필을 즉시 반환하는 목업(mock) 사용자 컨텍스트로 화면에 바로 진입할 수 있게 한다 — 실습마다 매번 로그인 절차를 거치지 않아도 되도록 하기 위함이다.
- 목업 사용자 컨텍스트는 나중에 실제 Firebase Auth 연동으로 교체할 때 호출부 코드를 바꾸지 않아도 되도록, 같은 인터페이스(예: 사용자 정보를 반환하는 훅 하나)를 유지한 채 내부 구현만 교체 가능한 형태로 만든다.

## 07. 리스크 · 오픈 이슈

- **이메일 미인증 가입** — 이메일 소유 확인 없이 가입이 가능해, 이론상 교사 이메일을 다른 사람이 먼저 선점 가입할 여지가 있다. 실사용 규모(교실 단위)에서는 우선순위 낮음, P2로 백로그 등록.
- **서버 부재로 인한 단일 장애점** — 접근 제어가 전적으로 Firestore 보안 규칙에 의존한다. 규칙 자체의 결함이 곧 보안 사고로 이어지므로, 규칙 변경 시 별도 검증 절차가 필요하다.

---
수업용 칸반보드 · 기획서 v1.0
